from setuptools import setup
setup(name='Petpy',
      version='0.1dev',
      packages=['petpy'],
      license='MIT License',
      long_description=open('README.md').read(),
      install_requires=['numpy>=1.11'],
      )

